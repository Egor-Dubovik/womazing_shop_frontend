import React, { FC } from 'react';

const MainPage: FC = () => {
  return (
    <div>
      MainPage
      {/* <button onClick={changeAuth}>click</button> */}
    </div>
  );
};

export default MainPage;
